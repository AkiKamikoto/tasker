// ─── Centralized app configuration ──────────────────────────────────────────
// Магические числа собраны здесь, чтобы их было легко настраивать
// и переиспользовать (в т.ч. под настраиваемый Pomodoro в Фазе 2).

export const POMODORO = {
  /** Длительность фокус-сессии, сек (25 мин) */
  focusSeconds: 25 * 60,
  /** Длительность короткого перерыва, сек (5 мин) */
  shortBreakSeconds: 5 * 60,
};

/** Как часто проверять задачи на наступление времени напоминания, мс */
export const NOTIFICATION_CHECK_INTERVAL_MS = 30_000;

/** Частоты звуковых сигналов Pomodoro, Гц */
export const BEEP = {
  focusEnd: 880,
  breakEnd: 587.33,
};
